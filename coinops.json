{
  "slug": "yoshwiki",
  "domain": "yoshi.science",
  "port": 8080,
  "type": "node",

  "deploy_cmd": "docker compose pull && docker compose down && docker compose up -d",
  "up_cmd": "docker compose up -d",
  "backup_cmd": "docker exec yoshwiki-db mariadb-dump -u root -p${DB_ROOT_PASSWORD} wikidb --single-transaction | gzip > ~/backups/yoshwiki/yoshwiki.sql.gz"
}
